# Development only — DO NOT use in production
VITE_GEMINI_API_KEY=

# Feature flag for LangGraph clinical workflow
VITE_USE_CLINICAL_GRAPH=false

# Production (server-side proxy) — see src/app/api/clinical/route.ts
GEMINI_API_KEY=

# Android-only backend override (Cloud Run / VPS)
VITE_ANDROID_BACKEND_URL=

# Freepik API for AI image generation
FREEPIK_API_KEY=
